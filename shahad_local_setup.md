# تشغيل شهد محلياً بدل السحابة + نسخ احتياطي
**عالطاير · 2 سبتمبر 2026**

---

## لماذا نغيّر أصلاً

الجلسة السحابية تعمل في حاوية مؤقتة تُمسح بعد فترة خمول. النتائج العملية اللي شفتها بنفسك:

| المشكلة | ما حصل فعلاً |
|---|---|
| الملفات تضيع | اضطررنا نعيد بناء الجلسة من الصفر |
| الصلاحيات ما تثبت | كل جلسة تبدأ بلا إعدادات |
| لا سجل لما أُرسل | ما نعرف مين تواصلنا معه إلا بملف يدوي |

محلياً: الملفات تبقى، الصلاحيات تثبت، والسجل يتراكم.

---

## ١. التثبيت المحلي

على جهازك (ماك أو ويندوز):

```bash
npm install -g @anthropic-ai/claude-code
```

ما عندك Node؟ نزّل من nodejs.org أولاً.

ثم سجّل دخولك بنفس حساب claude.ai:

```bash
claude
```

**الاتصالات (سلة وZapier/واتساب)** مربوطة بحسابك في claude.ai، فعلى الأرجح تنتقل تلقائياً. تأكد بأمر `/mcp` داخل الجلسة — إذا ما ظهرت، أعد ربطها من claude.ai ← الإعدادات ← Connectors.

---

## ٢. ⚠️ لا تضع بيانات العملاء في الريبو الحالي

`3altayer-images` **ريبو عام**. أسماء وأرقام ٤٧ عميل فيه = تسريب بيانات.

أنشئ ريبو **خاص** منفصل:

github.com/new ← الاسم: `3altayer-crm` ← اختر **Private** ← Create

ثم على جهازك:

```bash
git clone https://github.com/abdullahalra33-coder/3altayer-crm.git
cd 3altayer-crm
mkdir -p campaigns/ratings logs
```

انقل الملفات الأربعة (`shahad_playbook.md` · `shahad_messages.md` · `send_queue.csv` · `whatsapp_templates.md`) إلى `campaigns/ratings/`.

---

## ٣. النسخ الاحتياطي = commit بعد كل جلسة

```bash
git add -A && git commit -m "حملة التقييمات: تحديث $(date +%F)" && git push
```

هذا يعطيك:
- **نسخة خارج جهازك** (لو ضاع الجهاز، البيانات موجودة)
- **تاريخ كامل** — تشوف حالة أي عميل في أي يوم
- **تراجع** — لو انحذف شي بالغلط، ترجعه

### أفضل: نسخ تلقائي بعد كل تعديل

أنشئ `.claude/settings.json` داخل ريبو الـCRM:

```json
{
  "permissions": {
    "allow": [
      "mcp__Zapier__execute_zapier_write_action",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(git push:*)"
    ]
  },
  "hooks": {
    "Stop": [{
      "hooks": [{
        "type": "command",
        "command": "cd \"$CLAUDE_PROJECT_DIR\" && git add -A && git commit -q -m \"نسخة تلقائية $(date +%F_%H:%M)\" && git push -q 2>/dev/null || true"
      }]
    }]
  }
}
```

بهذا: كل مرة أنهي فيها ردّي، الملفات تُحفظ وتُرفع تلقائياً بدون ما تسوي شي.

> **ملاحظة:** ما جرّبت هذا الـhook — الحاوية السحابية منعتني من كتابة ملف صلاحيات. جرّبه محلياً وتأكد إنه يشتغل قبل ما تعتمد عليه.

---

## ٤. طبقة ثانية (اختيارية لكن منصوح بها)

الريبو الخاص يحميك من ضياع الجهاز. لكن لو انحذف الريبو أو ضاع الوصول للحساب؟

ضع ريبو الـCRM داخل مجلد iCloud / OneDrive / Google Drive. تصير عندك ثلاث نسخ: جهازك + GitHub + السحابة. تكلفة صفر.

---

## ٥. ما لا يحله التشغيل المحلي — بصراحة

**١) قوالب ميتا** لا تزال العائق. محلي أو سحابي، بدون قالب معتمد ما تنفتح محادثة مع عميل لم يراسلك.

**٢) الجلسة تبقى جلسة.** حتى محلياً، أنا لا أعمل وأنت نائم. للتشغيل التلقائي (رسالة تقييم بعد كل توصيل) الحل الصحيح **Zap مجدول في Zapier** يقرأ طلبات سلة ويرسل القالب — بدون وسيط. أنا أفيدك في الصياغة والتحليل والرد على الشكاوى، لا في كوني زر إرسال.

**٣) الصلاحيات ما زالت قرارك.** محلياً تقدر تضيفها بنفسك بأمر `/permissions` داخل الجلسة — أسهل بكثير من السحابة.

---

## الترتيب

1. ثبّت Claude Code محلياً
2. أنشئ ريبو `3altayer-crm` **خاص**
3. انقل الملفات الأربعة إليه
4. أضف `.claude/settings.json` وجرّب الـhook
5. ارفع قوالب ميتا (العائق الحقيقي)
6. بعد الاعتماد: قرّر — إرسال يدوي معي، أو Zap تلقائي
